
<% set_page_title("Vincular Usuario a Entidad") %>

<div class="row">
  <div class="col-12 card">
    <div class="card-body">
      <div class="m-2 col-12">
        <div class="container mx-auto my-8 px-4">
          <div class="max-w-xl mx-auto">
            <%= render 'form', group_user: @group_user %>
          </div>
        </div>

        <%= link_to 'Volver', group_users_path(group_id: @group.id), class: "btn btn-secondary m-4" %>
      </div>
    </div>
  </div>
</div>